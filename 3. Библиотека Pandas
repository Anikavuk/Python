# ЗАДАЧА 1 Получите доступ к библиотеке Pandas, используйте имя переменной pd.


import pandas as pd



# ЗАДАЧА 2 Создайте список со списками, где в каждом хранится пара «имя исполнителя - название трека».
# обязательно стаить запятые после квадратных скобок, внутри скобок без пробела и кавычки одинарные

music = [
    ['Валерия','Самолет'],
    ['Баста','Юлия'],
    ['Клава Кока','Забери пьяную'],
    ['Ольга Бузова','Мандаринки'],
    ['Егор Крид','Сердцеедка']
]


# ЗАДАЧА 3 Создайте список entries с названиями для двух столбцов — artist и track 
# (здесь эти английские слова употребляются в значении «исполнитель» и «композиция»).


entries = ['artist', 'track']

# ЗАДАЧА 4 Используя конструктор DataFrame(), создайте таблицу из списка ваших любимых 
# исполнителей music и списка столбцов entries. 
# Сохраните таблицу в переменной playlist и выведите эту сборную таблицу на экран.


playlist = pd.DataFrame(data=music , columns=entries) 
print(playlist)
