# Напишите функцию print_profit() (англ. profit, «прибыль»), которая вычисляет и выводит 
# на экран прибыль, полученную от фильма. Прибыль вычисляется как разность 
# сборов (выручки) и бюджета. Функция должна принимать значения бюджета и сборов как 
# аргументы (первый аргумент — бюджет, второй аргумент — сборы). Названия для аргументов выберите 
# самостоятельно. Результат должен печататься на экране с двумя знаками после запятой и без сопроводительного текста.
# В прекоде мы привели пример использования этой функции.


def print_profit(budget, sbor):
    profit = sbor - budget# < напишите функцию print_profit() >
    print("{:.2f}".format(profit))
print("Прибыль от фильма «Титаник»:")
print_profit(200.0, 2185.372302)
